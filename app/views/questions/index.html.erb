<h1>All Questions</h1>
<% @questions.each do |q| %>
  <p><%= link_to q.title, q %></p>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <% link_to q.title, q %>
      </h4>
    </div>
  </div>
<% end %>
